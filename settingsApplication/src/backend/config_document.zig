//! Canonical comment-preserving document implementation shared with the loader.
const shared = @import("display_config").document;
pub const Document = shared.Document;
pub const ConfigFiles = shared.ConfigFiles;
