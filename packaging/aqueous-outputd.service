[Unit]
Description=Aqueous wlr-randr output configuration sidecar
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/aqueous-outputd --serve
Restart=on-failure
RestartSec=2

[Install]
WantedBy=graphical-session.target
