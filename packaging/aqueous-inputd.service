[Unit]
Description=Aqueous libinput sidecar
Requires=aqueous-inputd.socket
After=aqueous-inputd.socket

[Service]
ExecStart=/usr/bin/aqueous-inputd
Restart=on-failure
RestartSec=1

[Install]
WantedBy=default.target
