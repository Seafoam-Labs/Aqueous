--- a/include/render/vulkan.h
+++ b/include/render/vulkan.h
@@ -5,6 +5,7 @@
 #include <string.h>
 #include <stdbool.h>
 #include <vulkan/vulkan.h>
+#include <wlr/render/vulkan.h>
 #include <wlr/render/wlr_renderer.h>
 #include <wlr/render/wlr_texture.h>
 #include <wlr/render/color.h>
@@ -428,6 +429,8 @@ struct wlr_vk_render_pass {
 	bool failed;
 	bool two_pass; // rendering via intermediate blending buffer
 	struct wlr_color_transform *color_transform;
+	wlr_vk_render_texture_hook_func_t texture_hook;
+	void *texture_hook_data;
 
 	struct wlr_drm_syncobj_timeline *signal_timeline;
 	uint64_t signal_point;
--- a/include/wlr/render/vulkan.h
+++ b/include/wlr/render/vulkan.h
@@ -10,14 +10,51 @@
 #define WLR_RENDER_VULKAN_H
 
 #include <vulkan/vulkan_core.h>
+#include <wlr/render/pass.h>
 #include <wlr/render/wlr_renderer.h>
 
+#define WLR_AQUEOUS_RENDER_HOOK_VERSION 3
+
 struct wlr_vk_image_attribs {
 	VkImage image;
 	VkImageLayout layout;
 	VkFormat format;
 };
 
+struct wlr_vk_render_pass_attribs {
+	VkCommandBuffer command_buffer;
+	VkRenderPass render_pass;
+	VkExtent2D extent;
+	uint32_t subpass;
+	bool has_signal_timeline;
+};
+
+struct wlr_vk_render_texture_attribs {
+	struct wlr_vk_render_pass_attribs render_pass;
+	VkPipelineLayout pipeline_layout;
+	VkDescriptorSet descriptor_set;
+	float projection[4][4];
+	float uv_offset[2];
+	float uv_size[2];
+	float color_matrix[4][4];
+	float alpha;
+	float luminance_multiplier;
+	uint32_t texture_transform;
+};
+
+typedef bool (*wlr_vk_render_texture_hook_func_t)(
+	const struct wlr_vk_render_texture_attribs *attribs, void *data);
+
+struct wlr_vk_render_offscreen_attribs {
+	VkCommandBuffer command_buffer;
+	VkImageView source_image_view;
+	VkFormat source_format;
+	VkExtent2D extent;
+};
+
+typedef bool (*wlr_vk_render_offscreen_func_t)(
+	const struct wlr_vk_render_offscreen_attribs *attribs, void *data);
+
 struct wlr_renderer *wlr_vk_renderer_create_with_drm_fd(int drm_fd);
 
 VkInstance wlr_vk_renderer_get_instance(struct wlr_renderer *renderer);
@@ -32,5 +69,36 @@ void wlr_vk_texture_get_image_attribs(struct wlr_texture *texture,
 	struct wlr_vk_image_attribs *attribs);
 bool wlr_vk_texture_has_alpha(struct wlr_texture *texture);
 
-#endif
+/**
+ * Get the active Vulkan command buffer and compatible render pass.
+ *
+ * This is only valid while a scene render hook is running. Retrieving the
+ * attributes invalidates wlroots' cached pipeline binding so callers may bind
+ * their own pipeline without corrupting later wlroots draws.
+ */
+bool wlr_vk_render_pass_get_attribs(struct wlr_render_pass *render_pass,
+	struct wlr_vk_render_pass_attribs *attribs);
 
+/**
+ * Install a callback for the next texture added to this Vulkan render pass.
+ *
+ * The callback receives wlroots' prepared texture descriptor, pipeline layout,
+ * transforms, color data, and active render-pass attributes. Returning true
+ * replaces wlroots' texture draw. Returning false uses the stock draw.
+ */
+bool wlr_vk_render_pass_set_texture_hook(struct wlr_render_pass *render_pass,
+	wlr_vk_render_texture_hook_func_t hook, void *data);
+
+/**
+ * Force the renderer to use its linear intermediate attachment.
+ */
+bool wlr_vk_renderer_enable_offscreen(struct wlr_renderer *renderer);
+
+/**
+ * Temporarily suspend the active pass and expose its completed linear color
+ * attachment to a callback. The callback may only read the source image view.
+ */
+bool wlr_vk_render_pass_run_offscreen(struct wlr_render_pass *render_pass,
+	wlr_vk_render_offscreen_func_t callback, void *data);
+
+#endif
--- a/include/wlr/types/wlr_scene.h
+++ b/include/wlr/types/wlr_scene.h
@@ -37,8 +37,13 @@ struct wlr_drag_icon;
 struct wlr_surface;
 
 struct wlr_scene_node;
+struct wlr_scene_rect;
 struct wlr_scene_buffer;
 struct wlr_scene_output_layout;
+struct wlr_render_pass;
+struct wlr_render_texture_options;
+struct wlr_render_rect_options;
+struct wlr_vk_render_texture_attribs;
 
 struct wlr_presentation;
 struct wlr_linux_dmabuf_v1;
@@ -49,6 +54,25 @@ struct wlr_output_state;
 typedef bool (*wlr_scene_buffer_point_accepts_input_func_t)(
 	struct wlr_scene_buffer *buffer, double *sx, double *sy);
 
+typedef bool (*wlr_scene_buffer_render_hook_func_t)(
+	struct wlr_scene_buffer *scene_buffer,
+	const struct wlr_render_texture_options *options,
+	const struct wlr_vk_render_texture_attribs *attribs, void *data);
+
+typedef bool (*wlr_scene_rect_render_hook_func_t)(
+	struct wlr_render_pass *render_pass, struct wlr_scene_rect *scene_rect,
+	const struct wlr_render_rect_options *options, void *data);
+
+typedef bool (*wlr_scene_buffer_needs_composition_func_t)(
+	struct wlr_scene_buffer *scene_buffer, void *data);
+
+typedef void (*wlr_scene_render_begin_hook_func_t)(
+	struct wlr_render_pass *render_pass, void *data);
+
+typedef void (*wlr_scene_node_render_hook_func_t)(
+	struct wlr_render_pass *render_pass, struct wlr_scene_node *node,
+	const pixman_region32_t *render_region, void *data);
+
 typedef void (*wlr_scene_buffer_iterator_func_t)(
 	struct wlr_scene_buffer *buffer, int sx, int sy, void *user_data);
 
@@ -270,6 +293,13 @@ struct wlr_scene_output {
 		uint64_t in_point;
 		struct wlr_drm_syncobj_timeline *out_timeline;
 		uint64_t out_point;
+
+		wlr_scene_buffer_render_hook_func_t buffer_render_hook;
+		wlr_scene_rect_render_hook_func_t rect_render_hook;
+		wlr_scene_buffer_needs_composition_func_t buffer_needs_composition;
+		wlr_scene_render_begin_hook_func_t render_begin_hook;
+		wlr_scene_node_render_hook_func_t node_render_hook;
+		void *render_hook_data;
 	} WLR_PRIVATE;
 };
 
@@ -595,6 +625,36 @@ void wlr_scene_output_destroy(struct wlr_scene_output *scene_output);
 void wlr_scene_output_set_position(struct wlr_scene_output *scene_output,
 	int lx, int ly);
 
+/**
+ * Set a callback which may replace ordinary scene-buffer texture draws.
+ */
+void wlr_scene_output_set_buffer_render_hook(
+	struct wlr_scene_output *scene_output,
+	wlr_scene_buffer_render_hook_func_t hook, void *data);
+
+/**
+ * Set the predicate used to disable direct scanout for affected buffers.
+ */
+void wlr_scene_output_set_buffer_needs_composition(
+	struct wlr_scene_output *scene_output,
+	wlr_scene_buffer_needs_composition_func_t predicate);
+
+/**
+ * Set a callback which may replace ordinary scene-rectangle draws.
+ */
+void wlr_scene_output_set_rect_render_hook(
+	struct wlr_scene_output *scene_output,
+	wlr_scene_rect_render_hook_func_t hook);
+
+/**
+ * Set callbacks invoked at the beginning of scene rendering and immediately
+ * before each visible scene node is rendered.
+ */
+void wlr_scene_output_set_render_hooks(
+	struct wlr_scene_output *scene_output,
+	wlr_scene_render_begin_hook_func_t begin_hook,
+	wlr_scene_node_render_hook_func_t node_hook);
+
 struct wlr_scene_output_state_options {
 	struct wlr_scene_timer *timer;
 
--- a/render/vulkan/pass.c
+++ b/render/vulkan/pass.c
@@ -5,6 +5,7 @@
 #include <wlr/util/log.h>
 #include <wlr/render/color.h>
 #include <wlr/render/drm_syncobj.h>
+#include <wlr/render/vulkan.h>
 
 #include "render/color.h"
 #include "render/vulkan.h"
@@ -19,6 +20,115 @@ static struct wlr_vk_render_pass *get_render_pass(struct wlr_render_pass *wlr_pa
 	return pass;
 }
 
+bool wlr_vk_render_pass_get_attribs(struct wlr_render_pass *wlr_pass,
+		struct wlr_vk_render_pass_attribs *attribs) {
+	if (wlr_pass == NULL || attribs == NULL ||
+			wlr_pass->impl != &render_pass_impl) {
+		return false;
+	}
+
+	struct wlr_vk_render_pass *pass = get_render_pass(wlr_pass);
+	struct wlr_buffer *buffer = pass->render_buffer->wlr_buffer;
+	pass->bound_pipeline = VK_NULL_HANDLE;
+	*attribs = (struct wlr_vk_render_pass_attribs){
+		.command_buffer = pass->command_buffer->vk,
+		.render_pass = pass->render_setup->render_pass,
+		.extent = {
+			.width = buffer->width,
+			.height = buffer->height,
+		},
+		.subpass = 0,
+		.has_signal_timeline = pass->signal_timeline != NULL,
+	};
+	return true;
+}
+
+bool wlr_vk_render_pass_set_texture_hook(struct wlr_render_pass *wlr_pass,
+		wlr_vk_render_texture_hook_func_t hook, void *data) {
+	if (wlr_pass == NULL || wlr_pass->impl != &render_pass_impl) {
+		return false;
+	}
+
+	struct wlr_vk_render_pass *pass = get_render_pass(wlr_pass);
+	pass->texture_hook = hook;
+	pass->texture_hook_data = data;
+	return true;
+}
+
+bool wlr_vk_renderer_enable_offscreen(struct wlr_renderer *wlr_renderer) {
+	if (!wlr_renderer_is_vk(wlr_renderer)) {
+		return false;
+	}
+
+	struct wlr_vk_renderer *renderer = vulkan_get_renderer(wlr_renderer);
+	renderer->offscreen_enabled = true;
+	return true;
+}
+
+bool wlr_vk_render_pass_run_offscreen(struct wlr_render_pass *wlr_pass,
+		wlr_vk_render_offscreen_func_t callback, void *data) {
+	if (wlr_pass == NULL || callback == NULL ||
+			wlr_pass->impl != &render_pass_impl) {
+		return false;
+	}
+
+	struct wlr_vk_render_pass *pass = get_render_pass(wlr_pass);
+	if (!pass->two_pass) {
+		return false;
+	}
+
+	VkCommandBuffer cb = pass->command_buffer->vk;
+	vkCmdNextSubpass(cb, VK_SUBPASS_CONTENTS_INLINE);
+	vkCmdEndRenderPass(cb);
+	pass->bound_pipeline = VK_NULL_HANDLE;
+
+	struct wlr_buffer *buffer = pass->render_buffer->wlr_buffer;
+	struct wlr_vk_render_offscreen_attribs attribs = {
+		.command_buffer = cb,
+		.source_image_view = pass->render_buffer->two_pass.blend_image_view,
+		.source_format = VK_FORMAT_R16G16B16A16_SFLOAT,
+		.extent = {
+			.width = buffer->width,
+			.height = buffer->height,
+		},
+	};
+	bool result = callback(&attribs, data);
+
+	VkImageMemoryBarrier barrier = {
+		.sType = VK_STRUCTURE_TYPE_IMAGE_MEMORY_BARRIER,
+		.srcAccessMask = VK_ACCESS_SHADER_READ_BIT,
+		.dstAccessMask = VK_ACCESS_COLOR_ATTACHMENT_READ_BIT |
+			VK_ACCESS_COLOR_ATTACHMENT_WRITE_BIT,
+		.oldLayout = VK_IMAGE_LAYOUT_SHADER_READ_ONLY_OPTIMAL,
+		.newLayout = VK_IMAGE_LAYOUT_COLOR_ATTACHMENT_OPTIMAL,
+		.srcQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED,
+		.dstQueueFamilyIndex = VK_QUEUE_FAMILY_IGNORED,
+		.image = pass->render_buffer->two_pass.blend_image,
+		.subresourceRange = {
+			.aspectMask = VK_IMAGE_ASPECT_COLOR_BIT,
+			.levelCount = 1,
+			.layerCount = 1,
+		},
+	};
+	vkCmdPipelineBarrier(cb, VK_PIPELINE_STAGE_FRAGMENT_SHADER_BIT,
+		VK_PIPELINE_STAGE_COLOR_ATTACHMENT_OUTPUT_BIT,
+		0, 0, NULL, 0, NULL, 1, &barrier);
+
+	VkRenderPassBeginInfo begin_info = {
+		.sType = VK_STRUCTURE_TYPE_RENDER_PASS_BEGIN_INFO,
+		.renderPass = pass->render_setup->render_pass,
+		.framebuffer = pass->render_buffer_out->framebuffer,
+		.renderArea.extent = attribs.extent,
+	};
+	vkCmdBeginRenderPass(cb, &begin_info, VK_SUBPASS_CONTENTS_INLINE);
+	vkCmdSetViewport(cb, 0, 1, &(VkViewport){
+		.width = attribs.extent.width,
+		.height = attribs.extent.height,
+		.maxDepth = 1,
+	});
+	return result;
+}
+
 static struct wlr_vk_color_transform *get_color_transform(
 		struct wlr_color_transform *c, struct wlr_vk_renderer *renderer) {
 	struct wlr_addon *a = wlr_addon_find(&c->addons, renderer, &vk_color_transform_impl);
@@ -919,16 +955,50 @@ static void render_pass_add_texture(struct wlr_render_pass *wlr_pass,
 	};
 	encode_color_matrix(color_matrix, frag_pcr_data.matrix);
 
-	bind_pipeline(pass, pipe->vk);
+	bool custom_draw = false;
+	if (pass->texture_hook != NULL) {
+		struct wlr_buffer *buffer = pass->render_buffer->wlr_buffer;
+		struct wlr_vk_render_texture_attribs attribs = {
+			.render_pass = {
+				.command_buffer = cb,
+				.render_pass = pass->render_setup->render_pass,
+				.extent = {
+					.width = buffer->width,
+					.height = buffer->height,
+				},
+				.subpass = 0,
+				.has_signal_timeline = pass->signal_timeline != NULL,
+			},
+			.pipeline_layout = pipe->layout->vk,
+			.descriptor_set = view->ds,
+			.alpha = frag_pcr_data.alpha,
+			.luminance_multiplier = frag_pcr_data.luminance_multiplier,
+			.texture_transform = tex_transform,
+		};
+		memcpy(attribs.projection, vert_pcr_data.mat4,
+			sizeof(attribs.projection));
+		memcpy(attribs.uv_offset, vert_pcr_data.uv_off,
+			sizeof(attribs.uv_offset));
+		memcpy(attribs.uv_size, vert_pcr_data.uv_size,
+			sizeof(attribs.uv_size));
+		memcpy(attribs.color_matrix, frag_pcr_data.matrix,
+			sizeof(attribs.color_matrix));
+		pass->bound_pipeline = VK_NULL_HANDLE;
+		custom_draw = pass->texture_hook(&attribs, pass->texture_hook_data);
+	}
+
+	if (!custom_draw) {
+		bind_pipeline(pass, pipe->vk);
 
-	vkCmdBindDescriptorSets(cb, VK_PIPELINE_BIND_POINT_GRAPHICS,
-		pipe->layout->vk, 0, 1, &view->ds, 0, NULL);
+		vkCmdBindDescriptorSets(cb, VK_PIPELINE_BIND_POINT_GRAPHICS,
+			pipe->layout->vk, 0, 1, &view->ds, 0, NULL);
 
-	vkCmdPushConstants(cb, pipe->layout->vk,
-		VK_SHADER_STAGE_VERTEX_BIT, 0, sizeof(vert_pcr_data), &vert_pcr_data);
-	vkCmdPushConstants(cb, pipe->layout->vk,
-		VK_SHADER_STAGE_FRAGMENT_BIT, sizeof(vert_pcr_data),
-		sizeof(frag_pcr_data), &frag_pcr_data);
+		vkCmdPushConstants(cb, pipe->layout->vk,
+			VK_SHADER_STAGE_VERTEX_BIT, 0, sizeof(vert_pcr_data), &vert_pcr_data);
+		vkCmdPushConstants(cb, pipe->layout->vk,
+			VK_SHADER_STAGE_FRAGMENT_BIT, sizeof(vert_pcr_data),
+			sizeof(frag_pcr_data), &frag_pcr_data);
+	}
 
 	pixman_region32_t clip;
 	get_clip_region(pass, options->clip, &clip);
@@ -936,10 +1006,12 @@ static void render_pass_add_texture(struct wlr_render_pass *wlr_pass,
 	int clip_rects_len;
 	const pixman_box32_t *clip_rects = pixman_region32_rectangles(&clip, &clip_rects_len);
 	for (int i = 0; i < clip_rects_len; i++) {
-		VkRect2D rect;
-		convert_pixman_box_to_vk_rect(&clip_rects[i], &rect);
-		vkCmdSetScissor(cb, 0, 1, &rect);
-		vkCmdDraw(cb, 4, 1, 0, 0);
+		if (!custom_draw) {
+			VkRect2D rect;
+			convert_pixman_box_to_vk_rect(&clip_rects[i], &rect);
+			vkCmdSetScissor(cb, 0, 1, &rect);
+			vkCmdDraw(cb, 4, 1, 0, 0);
+		}
 
 		struct wlr_box clip_box = {
 			.x = clip_rects[i].x1,
--- a/types/scene/wlr_scene.c
+++ b/types/scene/wlr_scene.c
@@ -24,6 +24,10 @@
 
 #include <wlr/config.h>
 
+#if WLR_HAS_VULKAN_RENDERER
+#include <wlr/render/vulkan.h>
+#endif
+
 #if WLR_HAS_XWAYLAND
 #include <wlr/xwayland/xwayland.h>
 #endif
@@ -1443,6 +1447,21 @@ struct render_list_entry {
 	int x, y;
 };
 
+#if WLR_HAS_VULKAN_RENDERER
+struct scene_buffer_render_hook_call {
+	struct wlr_scene_output *output;
+	struct wlr_scene_buffer *scene_buffer;
+	const struct wlr_render_texture_options *options;
+};
+
+static bool scene_buffer_render_hook_bridge(
+		const struct wlr_vk_render_texture_attribs *attribs, void *data) {
+	struct scene_buffer_render_hook_call *call = data;
+	return call->output->buffer_render_hook(call->scene_buffer, call->options,
+		attribs, call->output->render_hook_data);
+}
+#endif
+
 static float get_luminance_multiplier(const struct wlr_color_luminances *src_lum,
 		const struct wlr_color_luminances *dst_lum) {
 	return (dst_lum->reference / src_lum->reference) * (src_lum->max / dst_lum->max);
@@ -1485,7 +1504,7 @@ static void scene_entry_render(struct render_list_entry *entry, const struct ren
 	case WLR_SCENE_NODE_RECT:;
 		struct wlr_scene_rect *scene_rect = wlr_scene_rect_from_node(node);
 
-		wlr_render_pass_add_rect(data->render_pass, &(struct wlr_render_rect_options){
+		struct wlr_render_rect_options rect_options = {
 			.box = dst_box,
 			.color = {
 				.r = scene_rect->color[0],
@@ -1494,7 +1513,12 @@ static void scene_entry_render(struct render_list_entry *entry, const struct ren
 				.a = scene_rect->color[3],
 			},
 			.clip = &render_region,
-		});
+		};
+		if (data->output->rect_render_hook == NULL ||
+				!data->output->rect_render_hook(data->render_pass, scene_rect,
+					&rect_options, data->output->render_hook_data)) {
+			wlr_render_pass_add_rect(data->render_pass, &rect_options);
+		}
 		break;
 	case WLR_SCENE_NODE_BUFFER:;
 		struct wlr_scene_buffer *scene_buffer = wlr_scene_buffer_from_node(node);
@@ -1538,7 +1562,7 @@ static void scene_entry_render(struct render_list_entry *entry, const struct ren
 			WLR_COLOR_TRANSFER_FUNCTION_SRGB, &srgb_lum);
 		float luminance_multiplier = get_luminance_multiplier(&src_lum, &srgb_lum);
 
-		wlr_render_pass_add_texture(data->render_pass, &(struct wlr_render_texture_options) {
+		struct wlr_render_texture_options texture_options = {
 			.texture = texture,
 			.src_box = scene_buffer->src_box,
 			.dst_box = dst_box,
@@ -1556,7 +1580,26 @@ static void scene_entry_render(struct render_list_entry *entry, const struct ren
 			.luminance_multiplier = &luminance_multiplier,
 			.wait_timeline = scene_buffer->wait_timeline,
 			.wait_point = scene_buffer->wait_point,
-		});
+		};
+
+#if WLR_HAS_VULKAN_RENDERER
+		bool texture_hook_installed = false;
+		struct scene_buffer_render_hook_call hook_call = {
+			.output = data->output,
+			.scene_buffer = scene_buffer,
+			.options = &texture_options,
+		};
+		if (data->output->buffer_render_hook != NULL) {
+			texture_hook_installed = wlr_vk_render_pass_set_texture_hook(
+				data->render_pass, scene_buffer_render_hook_bridge, &hook_call);
+		}
+#endif
+		wlr_render_pass_add_texture(data->render_pass, &texture_options);
+#if WLR_HAS_VULKAN_RENDERER
+		if (texture_hook_installed) {
+			wlr_vk_render_pass_set_texture_hook(data->render_pass, NULL, NULL);
+		}
+#endif
 
 		struct wlr_scene_output_sample_event sample_event = {
 			.output = data->output,
@@ -1893,6 +1936,59 @@ void wlr_scene_output_set_position(struct wlr_scene_output *scene_output,
 	scene_output_update_geometry(scene_output, false);
 }
 
+void wlr_scene_output_set_buffer_render_hook(
+		struct wlr_scene_output *scene_output,
+		wlr_scene_buffer_render_hook_func_t hook, void *data) {
+	if (scene_output->buffer_render_hook == hook &&
+			scene_output->render_hook_data == data) {
+		return;
+	}
+
+	scene_output->buffer_render_hook = hook;
+	scene_output->render_hook_data = data;
+	wlr_damage_ring_add_whole(&scene_output->damage_ring);
+	wlr_output_schedule_frame(scene_output->output);
+}
+
+void wlr_scene_output_set_buffer_needs_composition(
+		struct wlr_scene_output *scene_output,
+		wlr_scene_buffer_needs_composition_func_t predicate) {
+	if (scene_output->buffer_needs_composition == predicate) {
+		return;
+	}
+
+	scene_output->buffer_needs_composition = predicate;
+	wlr_damage_ring_add_whole(&scene_output->damage_ring);
+	wlr_output_schedule_frame(scene_output->output);
+}
+
+void wlr_scene_output_set_rect_render_hook(
+		struct wlr_scene_output *scene_output,
+		wlr_scene_rect_render_hook_func_t hook) {
+	if (scene_output->rect_render_hook == hook) {
+		return;
+	}
+
+	scene_output->rect_render_hook = hook;
+	wlr_damage_ring_add_whole(&scene_output->damage_ring);
+	wlr_output_schedule_frame(scene_output->output);
+}
+
+void wlr_scene_output_set_render_hooks(
+		struct wlr_scene_output *scene_output,
+		wlr_scene_render_begin_hook_func_t begin_hook,
+		wlr_scene_node_render_hook_func_t node_hook) {
+	if (scene_output->render_begin_hook == begin_hook &&
+			scene_output->node_render_hook == node_hook) {
+		return;
+	}
+
+	scene_output->render_begin_hook = begin_hook;
+	scene_output->node_render_hook = node_hook;
+	wlr_damage_ring_add_whole(&scene_output->damage_ring);
+	wlr_output_schedule_frame(scene_output->output);
+}
+
 static bool scene_node_invisible(struct wlr_scene_node *node) {
 	if (node->type == WLR_SCENE_NODE_TREE) {
 		return true;
@@ -2443,7 +2524,18 @@ bool wlr_scene_output_build_state(struct wlr_scene_output *scene_output,
 	// - There are no color transforms that need to be applied
 	// - Damage highlight debugging is not enabled
 	enum scene_direct_scanout_result scanout_result = SCANOUT_INELIGIBLE;
-	if (options->color_transform == NULL && !render_gamma_lut && list_len == 1
+	bool render_hook_needs_composition = false;
+	if (list_len == 1 && scene_output->buffer_render_hook != NULL &&
+			list_data[0].node->type == WLR_SCENE_NODE_BUFFER) {
+		struct wlr_scene_buffer *scene_buffer =
+			wlr_scene_buffer_from_node(list_data[0].node);
+		render_hook_needs_composition =
+			scene_output->buffer_needs_composition == NULL ||
+			scene_output->buffer_needs_composition(scene_buffer,
+				scene_output->render_hook_data);
+	}
+	if (!render_hook_needs_composition &&
+			options->color_transform == NULL && !render_gamma_lut && list_len == 1
 			&& debug_damage != WLR_SCENE_DEBUG_DAMAGE_HIGHLIGHT) {
 		scanout_result = scene_entry_try_direct_scanout(&list_data[0], state, &render_data);
 	}
--- a/include/wlr/types/wlr_scene.h
+++ b/include/wlr/types/wlr_scene.h
@@ -143,6 +143,8 @@ struct wlr_scene_rect {
 	struct wlr_scene_node node;
 	int width, height;
 	float color[4];
+	bool force_blend;
+	bool input_enabled;
 };
 
 struct wlr_scene_outputs_update_event {
@@ -199,0 +202 @@ struct wlr_scene_buffer {
+	bool force_blend;
@@ -468,6 +471,18 @@ void wlr_scene_rect_set_size(struct wlr_scene_rect *rect, int width, int height);
  */
 void wlr_scene_rect_set_color(struct wlr_scene_rect *rect, const float color[static 4]);
 
+/**
+ * Force compositing for a rectangle whose custom shader may produce transparency.
+ */
+void wlr_scene_rect_set_force_blend(struct wlr_scene_rect *rect,
+	int force_blend);
+
+/**
+ * Control whether a rectangle participates in scene hit-testing.
+ */
+void wlr_scene_rect_set_input_enabled(struct wlr_scene_rect *rect,
+	int enabled);
+
 /**
  * Add a node displaying a buffer to the scene-graph.
  *
@@ -523,6 +538,12 @@ void wlr_scene_buffer_set_opaque_region(struct wlr_scene_buffer *scene_buffer,
 void wlr_scene_buffer_set_opaque_region(struct wlr_scene_buffer *scene_buffer,
 	const pixman_region32_t *region);
 
+/**
+ * Force compositing for a buffer whose custom shader may produce transparency.
+ */
+void wlr_scene_buffer_set_force_blend(struct wlr_scene_buffer *scene_buffer,
+	int force_blend);
+
 /**
  * Set the source rectangle describing the region of the buffer which will be
  * sampled to render this node. This allows cropping the buffer.
--- a/types/scene/wlr_scene.c
+++ b/types/scene/wlr_scene.c
@@ -263,11 +263,14 @@ static void scene_node_opaque_region(struct wlr_scene_node *node, int x, int y,
 
 	if (node->type == WLR_SCENE_NODE_RECT) {
 		struct wlr_scene_rect *scene_rect = wlr_scene_rect_from_node(node);
-		if (scene_rect->color[3] != 1) {
+		if (scene_rect->force_blend || scene_rect->color[3] != 1) {
 			return;
 		}
 	} else if (node->type == WLR_SCENE_NODE_BUFFER) {
 		struct wlr_scene_buffer *scene_buffer = wlr_scene_buffer_from_node(node);
+		if (scene_buffer->force_blend) {
+			return;
+		}
 
 		if (!scene_buffer->buffer) {
 			return;
@@ -774,0 +778 @@ struct wlr_scene_rect *wlr_scene_rect_create(struct wlr_scene_tree *parent,
+	scene_rect->input_enabled = true;
@@ -799,6 +803,21 @@ void wlr_scene_rect_set_color(struct wlr_scene_rect *rect, const float color[stat
 	scene_node_update(&rect->node, NULL);
 }
 
+void wlr_scene_rect_set_force_blend(struct wlr_scene_rect *rect,
+		int force_blend) {
+	if (rect->force_blend == (force_blend != 0)) {
+		return;
+	}
+
+	rect->force_blend = force_blend != 0;
+	scene_node_update(&rect->node, NULL);
+}
+
+void wlr_scene_rect_set_input_enabled(struct wlr_scene_rect *rect,
+		int enabled) {
+	rect->input_enabled = enabled != 0;
+}
+
 static void scene_buffer_handle_buffer_release(struct wl_listener *listener,
 		void *data) {
 	struct wlr_scene_buffer *scene_buffer =
@@ -1078,6 +1097,16 @@ void wlr_scene_buffer_set_opaque_region(struct wlr_scene_buffer *scene_buffer,
 	pixman_region32_fini(&update_region);
 }
 
+void wlr_scene_buffer_set_force_blend(struct wlr_scene_buffer *scene_buffer,
+		int force_blend) {
+	if (scene_buffer->force_blend == (force_blend != 0)) {
+		return;
+	}
+
+	scene_buffer->force_blend = force_blend != 0;
+	scene_node_update(&scene_buffer->node, NULL);
+}
+
 void wlr_scene_buffer_set_source_box(struct wlr_scene_buffer *scene_buffer,
 		const struct wlr_fbox *box) {
 	if (wlr_fbox_equal(&scene_buffer->src_box, box)) {
@@ -1405,0 +1440,7 @@ static bool scene_node_at_iterator(struct wlr_scene_node *node,
+
+	if (node->type == WLR_SCENE_NODE_RECT) {
+		struct wlr_scene_rect *scene_rect = wlr_scene_rect_from_node(node);
+		if (!scene_rect->input_enabled) {
+			return false;
+		}
+	}
--- a/include/render/vulkan.h
+++ b/include/render/vulkan.h
@@ -331,6 +331,7 @@
 	struct wl_list descriptor_pools; // wlr_vk_descriptor_pool.link
 	struct wl_list render_format_setups; // wlr_vk_render_format_setup.link
 
+	bool offscreen_enabled;
 
 	struct wl_list textures; // wlr_vk_texture.link
 	// Textures to return to foreign queue
--- a/render/vulkan/renderer.c
+++ b/render/vulkan/renderer.c
@@ -708,7 +708,8 @@
 		.tiling = VK_IMAGE_TILING_OPTIMAL,
 		.initialLayout = VK_IMAGE_LAYOUT_UNDEFINED,
 		.extent = (VkExtent3D) { dmabuf->width, dmabuf->height, 1 },
-		.usage = VK_IMAGE_USAGE_COLOR_ATTACHMENT_BIT | VK_IMAGE_USAGE_INPUT_ATTACHMENT_BIT,
+		.usage = VK_IMAGE_USAGE_COLOR_ATTACHMENT_BIT |
+			VK_IMAGE_USAGE_INPUT_ATTACHMENT_BIT | VK_IMAGE_USAGE_SAMPLED_BIT,
 	};
 
 	res = vkCreateImage(dev, &img_info, NULL, &buffer->two_pass.blend_image);
--- a/render/vulkan/pass.c
+++ b/render/vulkan/pass.c
@@ -1305,8 +1379,10 @@
 		inv_eotf = WLR_COLOR_TRANSFER_FUNCTION_GAMMA22;
 	}
 
-	bool using_linear_pathway = inv_eotf == WLR_COLOR_TRANSFER_FUNCTION_EXT_LINEAR;
-	bool using_srgb_pathway = inv_eotf == WLR_COLOR_TRANSFER_FUNCTION_SRGB &&
+	bool using_linear_pathway = !renderer->offscreen_enabled &&
+		inv_eotf == WLR_COLOR_TRANSFER_FUNCTION_EXT_LINEAR;
+	bool using_srgb_pathway = !renderer->offscreen_enabled &&
+		inv_eotf == WLR_COLOR_TRANSFER_FUNCTION_SRGB &&
 		buffer->srgb.out.framebuffer != VK_NULL_HANDLE;
 	bool using_two_pass_pathway = !using_linear_pathway && !using_srgb_pathway;
 
--- a/types/scene/wlr_scene.c
+++ b/types/scene/wlr_scene.c
@@ -1504,6 +1504,11 @@
 		return;
 	}
 
+	if (data->output->node_render_hook != NULL) {
+		data->output->node_render_hook(data->render_pass, node,
+			&render_region, data->output->render_hook_data);
+	}
+
 	int x = entry->x - data->logical.x;
 	int y = entry->y - data->logical.y;
 
@@ -2693,6 +2713,11 @@
 	});
 	pixman_region32_fini(&background);
 
+	if (scene_output->render_begin_hook != NULL) {
+		scene_output->render_begin_hook(render_pass,
+			scene_output->render_hook_data);
+	}
+
 	for (int i = list_len - 1; i >= 0; i--) {
 		struct render_list_entry *entry = &list_data[i];
 		scene_entry_render(entry, &render_data);
--- a/include/render/vulkan.h	2026-07-24 11:19:17.912154969 -0400
+++ b/include/render/vulkan.h	2026-07-24 10:27:31.164215845 -0400
@@ -282,0 +283 @@
+	struct wl_list completion_callbacks; // wlr_vk_render_completion.link
@@ -289,0 +291,6 @@
+};
+
+struct wlr_vk_render_completion {
+	struct wl_list link;
+	wlr_vk_render_completion_func_t callback;
+	void *data;
--- a/include/wlr/render/vulkan.h	2026-07-24 11:19:17.911753766 -0400
+++ b/include/wlr/render/vulkan.h	2026-07-24 10:27:31.134215399 -0400
@@ -16 +16 @@
-#define WLR_AQUEOUS_RENDER_HOOK_VERSION 3
+#define WLR_AQUEOUS_RENDER_HOOK_VERSION 7
@@ -57,0 +58,2 @@
+typedef void (*wlr_vk_render_completion_func_t)(void *data);
+
@@ -102,0 +105,9 @@
+
+/**
+ * Run a callback after the active pass' command buffer has completed.
+ *
+ * The callback is also run if recording or submission fails, making it safe
+ * to use for releasing resources retained by a custom draw.
+ */
+bool wlr_vk_render_pass_add_completion(struct wlr_render_pass *render_pass,
+	wlr_vk_render_completion_func_t callback, void *data);
--- a/include/wlr/types/wlr_scene.h	2026-07-24 11:19:17.912020752 -0400
+++ b/include/wlr/types/wlr_scene.h	2026-07-24 10:49:34.454574896 -0400
@@ -69,2 +69,3 @@
-typedef void (*wlr_scene_render_begin_hook_func_t)(
-	struct wlr_render_pass *render_pass, void *data);
+typedef uint32_t (*wlr_scene_render_begin_hook_func_t)(
+	struct wlr_render_pass *render_pass,
+	const struct wlr_box *damage_bounds, void *data);
--- a/render/vulkan/pass.c	2026-07-24 11:19:17.912283507 -0400
+++ b/render/vulkan/pass.c	2026-07-24 10:27:31.225216752 -0400
@@ -131,0 +132,20 @@
+bool wlr_vk_render_pass_add_completion(struct wlr_render_pass *wlr_pass,
+		wlr_vk_render_completion_func_t callback, void *data) {
+	if (wlr_pass == NULL || callback == NULL ||
+			wlr_pass->impl != &render_pass_impl) {
+		return false;
+	}
+
+	struct wlr_vk_render_completion *completion = calloc(1, sizeof(*completion));
+	if (completion == NULL) {
+		return false;
+	}
+	completion->callback = callback;
+	completion->data = data;
+
+	struct wlr_vk_render_pass *pass = get_render_pass(wlr_pass);
+	wl_list_insert(pass->command_buffer->completion_callbacks.prev,
+		&completion->link);
+	return true;
+}
+
--- a/render/vulkan/renderer.c	2026-07-24 11:19:17.912215888 -0400
+++ b/render/vulkan/renderer.c	2026-07-24 10:27:31.254217183 -0400
@@ -451,0 +452 @@
+	wl_list_init(&cb->completion_callbacks);
@@ -477,0 +479,8 @@
+	struct wlr_vk_render_completion *completion, *completion_tmp;
+	wl_list_for_each_safe(completion, completion_tmp,
+			&cb->completion_callbacks, link) {
+		wl_list_remove(&completion->link);
+		completion->callback(completion->data);
+		free(completion);
+	}
+
@@ -601,0 +611,8 @@
+
+	struct wlr_vk_render_completion *completion, *completion_tmp;
+	wl_list_for_each_safe(completion, completion_tmp,
+			&cb->completion_callbacks, link) {
+		wl_list_remove(&completion->link);
+		completion->callback(completion->data);
+		free(completion);
+	}
--- a/types/scene/wlr_scene.c	2026-07-24 11:19:17.912358576 -0400
+++ b/types/scene/wlr_scene.c	2026-07-24 10:49:34.484574233 -0400
@@ -2685,0 +2686,22 @@
+	if (scene_output->render_begin_hook != NULL) {
+		const pixman_box32_t *extents =
+			pixman_region32_extents(&render_data.damage);
+		struct wlr_box damage_bounds = {
+			.x = extents->x1,
+			.y = extents->y1,
+			.width = extents->x2 - extents->x1,
+			.height = extents->y2 - extents->y1,
+		};
+		uint32_t damage_expansion = scene_output->render_begin_hook(
+			render_pass, &damage_bounds, scene_output->render_hook_data);
+		if (damage_expansion > 0) {
+			wlr_region_expand(&render_data.damage, &render_data.damage,
+				damage_expansion);
+			pixman_region32_intersect_rect(&render_data.damage,
+				&render_data.damage, 0, 0, buffer->width, buffer->height);
+			// Keep backend damage synchronized with every pixel redrawn by
+			// the render hook, including blur-filter reach.
+			wlr_output_state_set_damage(state, &render_data.damage);
+		}
+	}
+
@@ -2728,5 +2746,0 @@
-
-	if (scene_output->render_begin_hook != NULL) {
-		scene_output->render_begin_hook(render_pass,
-			scene_output->render_hook_data);
-	}
--- a/include/wlr/render/vulkan.h
+++ b/include/wlr/render/vulkan.h
@@ -16 +16 @@
-#define WLR_AQUEOUS_RENDER_HOOK_VERSION 7
+#define WLR_AQUEOUS_RENDER_HOOK_VERSION 8
--- a/include/wlr/types/wlr_scene.h
+++ b/include/wlr/types/wlr_scene.h
@@ -71 +71 @@
-	const struct wlr_box *damage_bounds, void *data);
+	const pixman_region32_t *content_damage, void *data);
@@ -268,0 +269 @@
+		pixman_region32_t pending_effect_damage;
--- a/types/scene/wlr_scene.c
+++ b/types/scene/wlr_scene.c
@@ -376,2 +376,2 @@
-static void scene_output_damage(struct wlr_scene_output *scene_output,
-		const pixman_region32_t *damage) {
+static void scene_output_damage_internal(struct wlr_scene_output *scene_output,
+		const pixman_region32_t *damage, bool affects_scene) {
@@ -389,0 +390,4 @@
+		if (affects_scene) {
+			pixman_region32_union(&scene_output->pending_effect_damage,
+				&scene_output->pending_effect_damage, &clipped);
+		}
@@ -394,0 +399,5 @@
+static void scene_output_damage(struct wlr_scene_output *scene_output,
+		const pixman_region32_t *damage) {
+	scene_output_damage_internal(scene_output, damage, true);
+}
+
@@ -1796,0 +1806,2 @@
+			pixman_region32_subtract(&scene_output->pending_effect_damage,
+				&scene_output->pending_effect_damage, &state->damage);
@@ -1799,0 +1811,2 @@
+			pixman_region32_fini(&scene_output->pending_effect_damage);
+			pixman_region32_init(&scene_output->pending_effect_damage);
@@ -1840 +1853,3 @@
-	scene_output_damage(scene_output, &damage);
+	// Output-local presentation changes such as software cursor motion need
+	// repainting, but don't invalidate scene-derived effect caches.
+	scene_output_damage_internal(scene_output, &damage, false);
@@ -1862,0 +1878 @@
+	pixman_region32_init(&scene_output->pending_effect_damage);
@@ -1935,0 +1952 @@
+	pixman_region32_fini(&scene_output->pending_effect_damage);
@@ -2682,4 +2698,0 @@
-	pixman_region32_init(&render_data.damage);
-	wlr_damage_ring_rotate_buffer(&scene_output->damage_ring, buffer,
-		&render_data.damage);
-
@@ -2687,8 +2699,0 @@
-		const pixman_box32_t *extents =
-			pixman_region32_extents(&render_data.damage);
-		struct wlr_box damage_bounds = {
-			.x = extents->x1,
-			.y = extents->y1,
-			.width = extents->x2 - extents->x1,
-			.height = extents->y2 - extents->y1,
-		};
@@ -2696,9 +2701,17 @@
-			render_pass, &damage_bounds, scene_output->render_hook_data);
-		if (damage_expansion > 0) {
-			wlr_region_expand(&render_data.damage, &render_data.damage,
-				damage_expansion);
-			pixman_region32_intersect_rect(&render_data.damage,
-				&render_data.damage, 0, 0, buffer->width, buffer->height);
-			// Keep backend damage synchronized with every pixel redrawn by
-			// the render hook, including blur-filter reach.
-			wlr_output_state_set_damage(state, &render_data.damage);
+			render_pass, &scene_output->pending_effect_damage,
+			scene_output->render_hook_data);
+		if (damage_expansion > 0 &&
+				!pixman_region32_empty(&scene_output->pending_effect_damage)) {
+			pixman_region32_t expanded_effect_damage;
+			pixman_region32_init(&expanded_effect_damage);
+			wlr_region_expand(&expanded_effect_damage,
+				&scene_output->pending_effect_damage, damage_expansion);
+			pixman_region32_intersect_rect(&expanded_effect_damage,
+				&expanded_effect_damage, 0, 0, buffer->width, buffer->height);
+			wlr_damage_ring_add(&scene_output->damage_ring,
+				&expanded_effect_damage);
+			pixman_region32_union(&scene_output->pending_commit_damage,
+				&scene_output->pending_commit_damage, &expanded_effect_damage);
+			wlr_output_state_set_damage(state,
+				&scene_output->pending_commit_damage);
+			pixman_region32_fini(&expanded_effect_damage);
@@ -2706,0 +2720,4 @@
+
+	pixman_region32_init(&render_data.damage);
+	wlr_damage_ring_rotate_buffer(&scene_output->damage_ring, buffer,
+		&render_data.damage);
