# Aqueous upstream implementation handoff

Use [AQUEOUS-UPSTREAM-PLAN.md](AQUEOUS-UPSTREAM-PLAN.md) as the complete,
single-document handoff. Copy only that file into DankMaterialShell or
DankLinux-Docs and follow the section for the current repository.

It includes both plans, the shell/CLI and configuration contracts, the full JSON
schema, and the Wayland XML. All handoff references are embedded in the file.
