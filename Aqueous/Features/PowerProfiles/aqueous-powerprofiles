#!/bin/bash
SOCKET="${XDG_RUNTIME_DIR:-$HOME/.run}/aqueous-powerprofiles.sock"
if [ ! -S "$SOCKET" ]; then
    echo "Error: Power profiles service not running" >&2
    exit 1
fi
echo "$@" | socat - UNIX-CONNECT:"$SOCKET"
