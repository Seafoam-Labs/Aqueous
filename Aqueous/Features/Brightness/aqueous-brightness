#!/bin/bash
SOCKET="${XDG_RUNTIME_DIR:-$HOME/.run}/aqueous-brightness.sock"
if [ ! -S "$SOCKET" ]; then
    echo "Error: Brightness service not running" >&2
    exit 1
fi
echo "$@" | socat - UNIX-CONNECT:"$SOCKET"
